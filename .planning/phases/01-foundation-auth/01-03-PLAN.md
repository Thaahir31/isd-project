---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified: [tests/conftest.py, tests/test_main.py, tests/test_auth.py]
autonomous: true
requirements: [SETUP-04]
---

<objective>
Setup the automated testing suite using pytest and implement initial integration tests.

Purpose: To ensure long-term stability and verify the foundation and auth logic.
Output: Pytest configuration, test database setup, and core tests.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/main.py
@src/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pytest Configuration and Health-check Test</name>
  <files>tests/conftest.py, tests/test_main.py</files>
  <action>
    1. Create `tests/conftest.py` to define a `test_db` fixture (using a separate test database or SQLite in-memory for speed, but Postgres is preferred for parity).
    2. Define a `client` fixture using `TestClient` from FastAPI.
    3. Implement `tests/test_main.py` with a test for `GET /health`.
  </action>
  <verify>
    <automated>docker compose exec api pytest tests/test_main.py</automated>
  </verify>
  <done>Testing suite is configured and the health-check test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Auth and RBAC Integration Tests</name>
  <files>tests/test_auth.py</files>
  <action>
    1. Implement tests for the login flow: valid credentials, invalid credentials, missing fields.
    2. Implement tests for RBAC: Accessing Librarian-only endpoint with a Librarian token vs. Student token.
  </action>
  <verify>
    <automated>docker compose exec api pytest tests/test_auth.py</automated>
  </verify>
  <done>Authentication and RBAC logic are verified via automated integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test Runner → API | Tests bypass external network; verify internal logic. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | Test Logs | accept | Dev-only; ensure no production secrets are used in tests. |
</threat_model>

<verification>
Run all tests via 'docker compose exec api pytest' and ensure they pass.
</verification>

<success_criteria>
- Pytest is integrated and functional.
- Code coverage for Auth logic is established.
- Test database isolation is working.
</success_criteria>

<output>
Create .planning/phases/01-foundation-auth/01-03-SUMMARY.md when done
</output>
