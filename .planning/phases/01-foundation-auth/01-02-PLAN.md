---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/models/user.py, src/auth.py, src/schemas/user.py, src/api/auth.py, scripts/seed_librarian.py]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup:
  - service: local-env
    why: "Secret key for JWT"
    env_vars:
      - name: JWT_SECRET_KEY
        source: "Generate using `openssl rand -hex 32`"
---

<objective>
Implement JWT-based authentication and Role-Based Access Control (RBAC) with Student and Librarian roles.

Purpose: To secure the application and distinguish between user privileges.
Output: User models, login endpoint, RBAC dependencies, and a seeded Librarian user.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@src/main.py
@src/database.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: User Model and JWT Authentication</name>
  <files>src/models/user.py, src/auth.py, src/api/auth.py</files>
  <behavior>
    - User model should have email (unique), hashed_password, and role (Enum: student, librarian).
    - `POST /auth/login` should accept email/password and return a JWT access token.
    - `get_current_user` dependency should validate the JWT and return the User object.
  </behavior>
  <action>
    1. Create `User` model in `src/models/user.py` with roles.
    2. Implement password hashing using `passlib[bcrypt]`.
    3. Implement JWT generation and decoding using `python-jose`.
    4. Create `POST /auth/login` endpoint in `src/api/auth.py`.
    5. Register the auth router in `src/main.py`.
  </action>
  <verify>
    <automated>docker compose exec api pytest tests/test_auth.py::test_login</automated>
  </verify>
  <done>User can successfully log in and receive a valid JWT token.</done>
</task>

<task type="auto">
  <name>Task 2: RBAC Implementation and Seed Script</name>
  <files>src/auth.py, scripts/seed_librarian.py</files>
  <action>
    1. Implement `RoleChecker` dependency in `src/auth.py` to restrict endpoints by role.
    2. Add a test endpoint `GET /auth/librarian-only` protected by `RoleChecker(["librarian"])`.
    3. Create `scripts/seed_librarian.py` to create the first Librarian user in the database (Per user decision).
  </action>
  <verify>
    <automated>docker compose exec api python scripts/seed_librarian.py && curl -H "Authorization: Bearer $TOKEN" http://localhost:8000/auth/librarian-only</automated>
  </verify>
  <done>RBAC is functional and the first Librarian user is seeded in the database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | JWT provides stateless authentication. Compromised JWT = compromised session. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Spoofing | JWT Token | mitigate | Use strong JWT_SECRET_KEY and short-lived tokens (30 mins). |
| T-01-02-02 | Elevation of Privilege | RBAC Check | mitigate | Ensure role check is server-side and applies to all sensitive endpoints. |
</threat_model>

<verification>
1. Seed Librarian user.
2. Login to get token.
3. Access protected Librarian endpoint with token (expect 200).
4. Access protected Librarian endpoint with Student token or no token (expect 403/401).
</verification>

<success_criteria>
- JWT authentication is working.
- RBAC correctly distinguishes between Students and Librarians.
- Initial Librarian user exists.
</success_criteria>

<output>
Create .planning/phases/01-foundation-auth/01-02-SUMMARY.md when done
</output>
