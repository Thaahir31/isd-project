---
phase: 03-members
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/models/user.py, src/models/loan.py, src/models/__init__.py, src/schemas/user.py, src/api/members.py, src/main.py]
autonomous: true
requirements: [MEMBER-01]

must_haves:
  truths:
    - "Librarian can register a new student via API"
    - "Database includes student_id, department, and phone fields"
    - "Loans table exists with relations to User and Book"
  artifacts:
    - path: "src/models/loan.py"
      provides: "Loan model with status tracking"
    - path: "src/api/members.py"
      provides: "Admin registration endpoints"
    - path: "src/schemas/user.py"
      provides: "Member registration and view schemas"
  key_links:
    - from: "src/api/members.py"
      to: "src/models/user.py"
      via: "SQLAlchemy session"
---

<objective>
Establish the data foundation for members and loans, and implement Librarian-driven member registration.

Purpose: Extend the identity system to support university-specific member data and prepare for lending tracking.
Output: Updated User model, new Loan model, and member registration API.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-members/03-CONTEXT.md
@src/models/user.py
@src/auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Data Models & Migration</name>
  <files>src/models/user.py, src/models/loan.py, src/models/__init__.py, alembic/versions/</files>
  <action>
    1. Update `src/models/user.py`: Add `student_id` (unique, index), `department`, `phone`, `address` fields.
    2. Create `src/models/loan.py`: Implement `Loan` model with:
       - `id`: Integer PK
       - `book_id`: FK to books.id
       - `user_id`: FK to users.id
       - `status`: Enum (BORROWED, RETURNED, OVERDUE)
       - `borrowed_at`: DateTime (default now)
       - `due_date`: DateTime
       - `returned_at`: DateTime (nullable)
    3. Update `src/models/__init__.py`: Import `Loan` and `User` to ensure Alembic detects them.
    4. Generate and run migration: `alembic revision --autogenerate -m "add member and loan tables"` then `alembic upgrade head`.
  </action>
  <verify>
    <automated>python scripts/check_db.py --table users --column student_id; python scripts/check_db.py --table loans</automated>
  </verify>
  <done>User table extended and Loan table created in database.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Member Registration API</name>
  <files>src/schemas/user.py, src/api/members.py, src/main.py</files>
  <action>
    1. Create `src/schemas/user.py`: Define `MemberCreate` (username, email, password, student_id, department, phone, address) and `MemberView` schemas.
    2. Create `src/api/members.py`:
       - Implement `POST /members`: Restricted to LIBRARIAN role. Hashes password using `get_password_hash` from `src/auth.py`.
       - Implement `GET /members`: Librarian-only list of all users.
       - Implement `GET /members/{user_id}`: Librarian-only detail view.
    3. Register `members.router` in `src/main.py`.
  </action>
  <verify>
    <automated>pytest tests/test_members.py -k "test_librarian_can_register_member"</automated>
  </verify>
  <done>Librarian can register new students via API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Registration data (passwords, IDs) crosses here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | POST /members | mitigate | Enforce `require_role(UserRole.LIBRARIAN)` dependency |
| T-03-02 | Information Disclosure | GET /members | mitigate | Restrict all member list/detail endpoints to Librarians |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Check that a Librarian token allows registration of a student, while a Student token (if one exists) or no token results in 403/401.
</verification>

<success_criteria>
1. `alembic upgrade head` completes without error.
2. `POST /api/members` returns 201 Created for a valid request from a Librarian.
3. Registered student can successfully authenticate with `POST /api/auth/login`.
</success_criteria>

<output>
Create `.planning/phases/03-members/03-01-SUMMARY.md` when done
</output>
