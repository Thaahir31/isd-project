---
phase: 03-members
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/api/members.py, src/schemas/user.py, src/schemas/loan.py, tests/test_members.py]
autonomous: true
requirements: [MEMBER-02, MEMBER-03]

must_haves:
  truths:
    - "Student can view their own profile details"
    - "Student can update their phone number and address"
    - "Student can view their borrow history"
    - "Student cannot update their student_id or department"
  artifacts:
    - path: "src/schemas/loan.py"
      provides: "Loan schemas for history view"
  key_links:
    - from: "src/api/members.py"
      to: "src/models/loan.py"
      via: "Query filter by user_id"
---

<objective>
Implement student self-service capabilities for profile management and borrow history visualization.

Purpose: Empower library members to manage their contact data and track their own lending activity.
Output: Profile view/update endpoints and borrow history API.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-members/03-01-SUMMARY.md
@src/api/members.py
@src/models/loan.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile View and Update</name>
  <files>src/api/members.py, src/schemas/user.py</files>
  <action>
    1. Update `src/schemas/user.py`: Add `MemberUpdate` schema (phone, address - both optional).
    2. Update `src/api/members.py`:
       - Implement `GET /members/me`: Uses `get_current_user` to return the profile of the authenticated member.
       - Implement `PATCH /members/me`: Allows the current user to update their `phone` and `address`. 
       - Ensure `PATCH /members/{user_id}` (Admin) allows updating all fields including `student_id` and `department`.
  </action>
  <verify>
    <automated>pytest tests/test_members.py -k "test_student_can_update_profile"</automated>
  </verify>
  <done>Students can view and update their own contact information.</done>
</task>

<task type="auto">
  <name>Task 2: Borrow History API</name>
  <files>src/api/members.py, src/schemas/loan.py</files>
  <action>
    1. Create `src/schemas/loan.py`: Define `LoanView` schema (includes book title, status, dates).
    2. Update `src/api/members.py`:
       - Implement `GET /members/me/history`: Returns a list of `Loan` records for the current user, joined with `Book` for titles.
       - Implement `GET /members/{user_id}/history`: Librarian-only access to any student's history.
  </action>
  <verify>
    <automated>pytest tests/test_members.py -k "test_student_can_view_history"</automated>
  </verify>
  <done>Borrow history is accessible via API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Student -> Profile API | Update data crosses here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | PATCH /members/me | mitigate | Use Pydantic schema to white-list updatable fields; ignore student_id/department in student-facing PATCH |
| T-03-04 | Information Disclosure | GET /members/{id} | mitigate | Ensure non-Librarians can only access their own ID |
</threat_model>

<verification>
Verify that a student can update their phone number and see it reflected in `/members/me`. Verify that a student trying to update their `department` via `/members/me` fails or has no effect.
</verification>

<success_criteria>
1. `GET /api/members/me` returns the current user's profile.
2. `PATCH /api/members/me` successfully updates phone/address for students.
3. `GET /api/members/me/history` returns an empty list (since no loans exist yet) with 200 OK.
</success_criteria>

<output>
Create `.planning/phases/03-members/03-02-SUMMARY.md` when done
</output>
