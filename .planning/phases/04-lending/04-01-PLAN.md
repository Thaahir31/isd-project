---
phase: 04-lending
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/models/loan_request.py, src/schemas/loan_request.py, src/api/loans.py, src/main.py]
autonomous: true
requirements: [LENDING-01, LENDING-04]

must_haves:
  truths:
    - "Student can submit a loan request for a book"
    - "System prevents more than 3 active loans/requests per student"
    - "Librarians can view all pending requests"
    - "Librarians can deny a request"
    - "Pending requests older than 3 days can be marked as EXPIRED"
  artifacts:
    - path: "src/models/loan_request.py"
      provides: "LoanRequest database model"
    - path: "src/api/loans.py"
      provides: "Endpoints for loan request lifecycle"
  key_links:
    - from: "src/api/loans.py"
      to: "src/models/loan_request.py"
      via: "SQLAlchemy queries"
---

<objective>
Implement the initial Loan Request workflow, allowing students to express interest in books and librarians to manage these requests, including automatic expiry logic.

Purpose: To establish the 'Pending' state of the lending lifecycle and enforce student borrowing limits.
Output: LoanRequest model, request creation API, librarian management API, and expiry utility.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-lending/04-CONTEXT.md
@src/models/book.py
@src/models/user.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: LoanRequest Model & Status Enums</name>
  <files>src/models/loan_request.py, src/models/__init__.py, alembic/versions/</files>
  <action>
    Create the `LoanRequest` model and `LoanRequestStatus` enum in `src/models/loan_request.py`.
    - Enums: PENDING, APPROVED, DENIED, EXPIRED.
    - Fields: id, user_id (FK), book_id (FK), status, created_at, expires_at (default created_at + 3 days).
    - Register model in `src/models/__init__.py`.
    - Generate and run an Alembic migration to create the table.
  </action>
  <verify>
    <automated>pytest tests/test_models.py (create a temporary test to verify model instantiation and migration success)</automated>
  </verify>
  <done>LoanRequest table exists in the database with correct relationships to User and Book.</done>
</task>

<task type="auto">
  <name>Task 2: Student Request API</name>
  <files>src/api/loans.py, src/schemas/loan_request.py, src/main.py</files>
  <action>
    - Create `src/schemas/loan_request.py` with Pydantic models for request creation and response.
    - Create `src/api/loans.py` and register it in `src/main.py`.
    - Implement `POST /api/loans/requests`:
      - Requires `STUDENT` role.
      - Checks if the book exists.
      - Checks if the student already has 3 active items (Pending Requests + Active Loans). *Note: Loan model doesn't exist yet, so check LoanRequest status=PENDING/APPROVED for now, but leave a TODO for Loans.*
      - Creates a `LoanRequest` with status `PENDING`.
  </action>
  <verify>
    <automated>curl -X POST /api/loans/requests -H "Authorization: Bearer student_token" -d '{"book_id": 1}'</automated>
  </verify>
  <done>Students can successfully create loan requests subject to the 3-item limit.</done>
</task>

<task type="auto">
  <name>Task 3: Librarian Management & Expiry Logic</name>
  <files>src/api/loans.py</files>
  <action>
    - Implement `GET /api/loans/requests`:
      - Requires `LIBRARIAN` role.
      - Returns list of pending requests.
    - Implement `POST /api/loans/requests/{request_id}/deny`:
      - Requires `LIBRARIAN` role.
      - Updates status to `DENIED`.
    - Implement `POST /api/loans/requests/cleanup-expired`:
      - Internal or Librarian-only endpoint to update status to `EXPIRED` for any `PENDING` request where `expires_at` < now.
  </action>
  <verify>
    <automated>pytest tests/test_lending_requests.py (Create test suite for list, deny, and expiry logic)</automated>
  </verify>
  <done>Librarians can view and deny requests, and expired requests can be cleaned up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Student -> API | User submitting request for a book |
| Librarian -> API | User managing requests |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | loan_request.status | mitigate | Ensure only librarians can change status to APPROVED/DENIED/EXPIRED via RBAC |
| T-04-02 | Denial of Service | requests table | mitigate | Enforce 3-item limit per student to prevent flooding |
</threat_model>

<verification>
Verify that a student can create a request, a second student can create one, and the librarian can see both and deny one.
</verification>

<success_criteria>
- LoanRequest model is implemented and migrated.
- Student can request books up to a limit of 3.
- Librarian can view and deny requests.
- Expiry logic correctly identifies and updates old requests.
</success_criteria>

<output>
Create `.planning/phases/04-lending/04-01-SUMMARY.md` when done
</output>
