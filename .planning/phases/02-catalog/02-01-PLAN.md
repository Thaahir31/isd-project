---
phase: 02-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, src/models/author.py, src/models/book.py, alembic/versions/...]
autonomous: true
requirements: [CATALOG-01, CATALOG-02]

must_haves:
  truths:
    - "Author and Book tables exist in the database with correct relationships"
    - "Librarian can create, read, update, and delete authors"
  artifacts:
    - path: "src/models/author.py"
      provides: "Author model"
    - path: "src/models/book.py"
      provides: "Book model with FK to Author"
    - path: "src/api/authors.py"
      provides: "Author CRUD endpoints"
  key_links:
    - from: "src/models/book.py"
      to: "src/models/author.py"
      via: "author_id foreign key"
---

<objective>
Establish the foundational data models for the catalog and implement Author management.

Purpose: Provide the relational structure for books and their creators.
Output: Author and Book models, database migration, and Author CRUD API.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-catalog/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog Models & Migration</name>
  <files>requirements.txt, src/models/author.py, src/models/book.py, src/main.py</files>
  <action>
    - Add `python-stdnum` to `requirements.txt`.
    - Create `src/models/author.py` with fields: id, name, biography (optional).
    - Create `src/models/book.py` with fields: id, title, author_id (FK), isbn, total_copies, available_copies, category.
    - Ensure `available_copies` defaults to `total_copies` (per D-F-004).
    - Register new models in `src/main.py` or wherever models are imported for Alembic.
    - Run `alembic revision --autogenerate -m "add catalog tables"` and `alembic upgrade head`.
  </action>
  <verify>
    <automated>python scripts/check_db.py --tables authors,books</automated>
  </verify>
  <done>Database schema updated with authors and books tables.</done>
</task>

<task type="auto">
  <name>Task 2: Author CRUD API</name>
  <files>src/api/authors.py, src/schemas/author.py, src/main.py</files>
  <action>
    - Create Pydantic schemas for Author (Create, Update, Response).
    - Implement endpoints in `src/api/authors.py`:
      - POST /authors (Librarian only)
      - GET /authors (Public/Student)
      - GET /authors/{id} (Public/Student)
      - PUT /authors/{id} (Librarian only)
      - DELETE /authors/{id} (Librarian only)
    - Include role-based protection using dependencies from `src/auth.py`.
    - Register the router in `src/main.py`.
  </action>
  <verify>
    <automated>pytest tests/test_authors.py</automated>
  </verify>
  <done>Author CRUD endpoints functional and role-protected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input in author name and biography |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Author CRUD | mitigate | Enforce Librarian role check using JWT dependency |
| T-02-SC | Tampering | python-stdnum | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] |
</threat_model>

<verification>
Check database for new tables and verify Author CRUD via automated tests.
</verification>

<success_criteria>
Author management is fully functional and restricted to Librarians for write operations.
</success_criteria>

<output>
Create `.planning/phases/02-catalog/02-01-SUMMARY.md` when done
</output>
