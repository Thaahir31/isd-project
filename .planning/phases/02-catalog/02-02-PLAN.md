---
phase: 02-catalog
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/api/books.py, src/schemas/book.py, src/logic/isbn.py, src/main.py]
autonomous: true
requirements: [CATALOG-01]

must_haves:
  truths:
    - "Librarian can create, read, update, and delete books"
    - "Books are validated for ISBN-13 format before creation"
  artifacts:
    - path: "src/logic/isbn.py"
      provides: "ISBN validation utility"
    - path: "src/api/books.py"
      provides: "Book CRUD endpoints"
  key_links:
    - from: "src/api/books.py"
      to: "src/logic/isbn.py"
      via: "validation call in POST/PUT"
---

<objective>
Implement Book management with robust ISBN validation.

Purpose: Manage the physical inventory and ensure data quality via ISBN checks.
Output: ISBN validation logic and Book CRUD API.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-catalog/02-01-SUMMARY.md
@.planning/phases/02-catalog/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ISBN Validation Utility</name>
  <files>src/logic/isbn.py</files>
  <action>
    - Implement `validate_isbn13(isbn: str) -> bool` using `python-stdnum.isbn.is_valid`.
    - Ensure it handles both dashed and non-dashed input by normalizing first.
  </action>
  <verify>
    <automated>pytest tests/test_isbn.py</automated>
  </verify>
  <done>ISBN-13 validation logic is accurate and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Book CRUD API</name>
  <files>src/api/books.py, src/schemas/book.py, src/main.py</files>
  <action>
    - Create Pydantic schemas for Book (Create, Update, Response).
    - Use a custom validator in the Pydantic schema to call `validate_isbn13`.
    - Implement endpoints in `src/api/books.py`:
      - POST /books (Librarian only)
      - GET /books (Public/Student)
      - GET /books/{id} (Public/Student)
      - PUT /books/{id} (Librarian only)
      - DELETE /books/{id} (Librarian only)
    - Ensure Book creation initializes `available_copies` = `total_copies`.
    - Handle 404 if author_id does not exist.
    - Register the router in `src/main.py`.
  </action>
  <verify>
    <automated>pytest tests/test_books.py</automated>
  </verify>
  <done>Book CRUD endpoints functional, role-protected, and ISBN-validated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input in ISBN and title fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Spoofing | Book CRUD | mitigate | Enforce Librarian role check using JWT dependency |
| T-02-03 | Integrity | ISBN field | mitigate | Validate ISBN format using python-stdnum before persistence |
</threat_model>

<verification>
Verify ISBN validation for edge cases and Book CRUD via automated tests.
</verification>

<success_criteria>
Book management is fully functional and ensures high data integrity for ISBNs.
</success_criteria>

<output>
Create `.planning/phases/02-catalog/02-02-SUMMARY.md` when done
</output>
