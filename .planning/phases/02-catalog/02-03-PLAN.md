---
phase: 02-catalog
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [src/api/books.py, src/models/book.py]
autonomous: true
requirements: [CATALOG-03, CATALOG-04]

must_haves:
  truths:
    - "Search returns results matching title, author name, or ISBN"
    - "Search results are returned in < 1 second"
    - "Each book record accurately reflects available vs total copies"
  artifacts:
    - path: "src/api/books.py"
      provides: "Search endpoint"
  key_links:
    - from: "src/api/books.py"
      to: "src/models/author.py"
      via: "Join query for Author Name search"
---

<objective>
Implement high-performance search and availability status reporting.

Purpose: Enable users to find books easily and see if they can be borrowed.
Output: Search API and availability reporting logic.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-catalog/02-02-SUMMARY.md
@.planning/phases/02-catalog/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog Search API</name>
  <files>src/api/books.py</files>
  <action>
    - Implement `GET /books/search` endpoint.
    - Accept query parameter `q` (string).
    - Use SQLAlchemy `join` between Book and Author.
    - Filter using `ILIKE` on `Book.title`, `Author.name`, and `Book.isbn`.
    - Per D-F-003, use exact match for ISBN if it looks like one, or include it in ILIKE.
    - Ensure pagination is implemented to maintain performance.
  </action>
  <verify>
    <automated>pytest tests/test_search.py</automated>
  </verify>
  <done>Catalog search is functional and performant across title, author, and ISBN.</done>
</task>

<task type="auto">
  <name>Task 2: Availability & Performance Verification</name>
  <files>tests/test_search_perf.py, src/api/books.py</files>
  <action>
    - Verify that `available_copies` is included in all Book response schemas.
    - Write a performance test that seeds 1000 books and verifies search latency is < 1s (per D-F-003).
    - Add database indexes on `Book.title` and `Book.isbn` if performance fails (optional/discretionary).
  </action>
  <verify>
    <automated>pytest tests/test_search_perf.py</automated>
  </verify>
  <done>Search performance target met and availability reporting verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted search query string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | Search API | accept | Search data is public library catalog |
| T-02-05 | Denial of Service | Complex search | mitigate | Implement pagination and limit search query length |
</threat_model>

<verification>
Verify search accuracy and performance via automated tests.
</verification>

<success_criteria>
Catalog is searchable by all users with sub-second response times.
</success_criteria>

<output>
Create `.planning/phases/02-catalog/02-03-SUMMARY.md` when done
</output>
